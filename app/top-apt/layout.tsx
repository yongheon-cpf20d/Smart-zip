export const metadata = {
  title: "지역별 대장아파트 순위 | 똑집",
  description: "같은 지역, 같은 평형대 아파트를 실거래 최고가 순으로 보는 대장아파트 랭킹.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}